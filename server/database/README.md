# 💾 Database Scripts

SQL scripts for database setup and migrations.

## 📄 Files

- **`schema.sql`** - Main database schema (tables, structure)
- **`seed_zip_locations.sql`** - Seed data for zip code locations
- **`*.sql`** - Migration scripts for adding features

## ⚠️ Note

These are historical migration files. The main schema is in `schema.sql`.
